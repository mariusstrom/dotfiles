- defaults:
            link:
                          relink: true

- clean: ['~']

- link:
        ~/.dotfiles: ''
        ~/.poshthemes: 
        ~/.vimrc:
        ~/.zshrc:
        ~/.zsh:
        # ~/.config: 
        ~/.gitconfig:
        ~/.tmux.conf:

- shell:
          - [git submodule update --init --recursive, Installing submodules]
