- defaults:
            link:
                          relink: true

- clean: ['~']

- link:
        ~/.dotfiles: ''
        ~/.poshthemes: 
        ~/.vim:
        ~/.vimrc:
        ~/.zsh:
        ~/.zshrc:
        # ~/.config: 
        ~/.gitconfig:
        ~/.tmux.conf:

- shell:
          - [git submodule update --init --recursive, Installing submodules]
