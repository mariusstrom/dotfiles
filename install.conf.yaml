- defaults:
            link:
                          relink: true

- clean: ['~']

- link:
        ~/.dotfiles: ''
        ~/.vim:
        ~/.vimrc:
        ~/.zsh:
        ~/.zshrc:
        ~/.config: 
        ~/.gitconfig:

- shell:
          - [git submodule update --init --recursive, Installing submodules]
